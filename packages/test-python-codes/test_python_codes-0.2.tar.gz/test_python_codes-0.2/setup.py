from setuptools import setup, find_packages

setup(
    name='test_python_codes',
    version='0.2',
    packages=find_packages(),  # This should automatically include your folder
    install_requires=[],
)
