[project]
name = "net2i"
version = "0.1.0"
description = "A Python package to convert network traffic data to image representations."
authors = [{ name = "Dr. Omesh Fernando", email = "omeshf@gmail.com" }]
license = { text = "MIT" }  # Or { file = "LICENSE" }

[build-system]
requires = ["setuptools>=40.8.0", "wheel"]
build-backend = "setuptools.build_meta"
