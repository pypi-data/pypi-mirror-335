# pyproject.toml

[build-system]
requires = ["setuptools>=65.5.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "latex_images_and_tables_generator"
version = "0.1.0"
description = "A library for generating LaTeX documents with tables and images."
dependencies = []

[tool.setuptools.packages.find]
where = ["."]