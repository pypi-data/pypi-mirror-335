
[tool.poetry]
name = "hw-2-firepancake"
version = "0.1.2"
description = "Simple functional LaTeX code generator (tables, figures)"
authors = ["Anton Poiskov <poiskov2016@gmail.com>"]
license = "MIT"
packages = [{ include = "hw2", from = "src" }]


[tool.poetry.dependencies]
python = ">=3.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"