[project]
name = "biocompart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "polars>=1.25.2",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
