from setuptools import setup, find_packages

setup(
    name='colorstreak',
    version='0.1.0',
    description='Terminal color formatting for Python',
    author='Fernando Leon Franco',
    packages=find_packages(),
    install_requires=[],  # Lista de dependencias, si las hay
)
