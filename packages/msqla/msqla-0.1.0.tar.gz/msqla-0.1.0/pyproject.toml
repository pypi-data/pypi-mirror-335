[project]
name = "msqla"
version = "0.1.0"
description = "Next-level sharded queries with sqlalchemy."
readme = "README.md"
authors = [
    { name = "julianvanmeggelen", email = "julianvanmeggelen@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "sqlalchemy>=2.0.39",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
