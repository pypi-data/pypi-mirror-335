[project]
name = "mcp-smarthub"
version = "0.1.2"
description = "MCP Server for SmartHub"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
