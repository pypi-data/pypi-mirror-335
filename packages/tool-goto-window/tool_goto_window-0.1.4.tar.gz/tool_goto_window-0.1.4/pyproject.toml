[project]
name = "tool-goto-window"
version = "0.1.4"
description = "A CLI tool to switch between windows"
readme = "README.md"
authors = [
    { name = "Eyal Levin", email = "eyalev@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "typer",
    "pywinctl",
    "toml",
]

[project.scripts]
tool-goto-window = "tool_goto_window.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
