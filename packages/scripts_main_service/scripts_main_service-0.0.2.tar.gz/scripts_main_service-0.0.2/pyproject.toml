[project]
name = "scripts_main_service"
version = "0.0.2"
description = ""
authors = [
    {name = "jybaby1027",email = "18523852976@126.com"}
]
readme = "README.md"
requires-python = ">=3.11, <4.0"
dynamic = ["dependencies"]

[tool.poetry.dependencies]
pytest = "^8.3.5"
facebook_plugin_banyan = ">=0.0.1"
xcmap = ">=0.0.1"

[project.optional-dependencies]
jupyter = ["jupyter>=1.1.0, < 2.0.0"]

[tool.poetry]
packages = [{include = "scripts_main_service", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
