[project]
name = "fewsats-mcp"
version = "0.0.3"
description = "Fewsats MCP server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.4.1",
    "fewsats>=0.0.14",
]

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
sherlock-mcp = "fewsats_mcp:main"
