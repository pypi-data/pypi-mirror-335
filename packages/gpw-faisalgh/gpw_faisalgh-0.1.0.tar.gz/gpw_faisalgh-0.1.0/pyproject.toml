[project]
name = "gpw-faisalgh"
version = "0.1.0"
description = ""
authors = [
    {name = "faisalgh", email = "faisalgh06@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13,<4.0"
dependencies = [
    "flask==3.0.0",
    "langchain-ollama==0.1.0",
    "requests (>=2.32.3,<3.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
packages = [
    { include = "gpw" }  # Include the "gpw" folder as a package
]