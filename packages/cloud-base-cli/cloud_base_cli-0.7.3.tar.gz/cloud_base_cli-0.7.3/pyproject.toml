[tool.poetry]
name = "cloud-base-cli"
version = "0.7.3"
description = "Cloud Base ClI App for LZ"
authors = ["Altair Penaredondo <apenaredondo@stratpoint.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.1.6"
typer = "^0.9.0"
python-gitlab = "5.3.0"
gitpython = "3.1.42"
boto3 = "1.34.48"

[tool.poetry.scripts]
cloud-base-cli = "cloud_base_cli.cli:app"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.3"
pre-commit = "^3.5.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
