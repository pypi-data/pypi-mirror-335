[tool.poetry]
name = "pa-latex"
version = "0.1.0"
description = "A sample package to work with latex"
authors = ["Semyon Lukin <lukinsemyon@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pdflatex = "^0.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
