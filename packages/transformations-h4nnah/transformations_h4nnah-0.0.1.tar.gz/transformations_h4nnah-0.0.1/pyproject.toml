[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[project]
name = "transformations_h4nnah"
version = "0.0.1"
authors = [
  { name="Hannah Mänd", email="hannah@python.com" },
]
description = "Koordinaatide konvertor/transformer"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
Homepage = "https://github.com/pypa/sampleproject"
Issues = "https://github.com/pypa/sampleproject/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/calculations_h4nnah"]