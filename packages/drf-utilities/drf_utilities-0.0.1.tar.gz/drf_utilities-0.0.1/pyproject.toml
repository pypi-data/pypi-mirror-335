[project]
name = "drf_utilities"
version = "0.0.1"
description = "Extended utilities for django rest framework"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0", "djangorestframework"]
build-backend = "setuptools.build_meta"