[project]
authors = [{name = "leonid", email = "leonid.alhimenkov@yandex.ru"}]
dependencies = []
name = "latex_lib1243125"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.project]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
latex_lib1243125 = { path = ".", editable = true }
build = "*"
setuptools = "*"
wheel = "*"
twine = "*"

[tool.pixi.tasks]

[tool.pixi.dependencies]
ruff = ">=0.11.0,<0.12"
