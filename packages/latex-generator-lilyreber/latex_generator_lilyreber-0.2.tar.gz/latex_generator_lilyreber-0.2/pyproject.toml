[project]
name = "latex_generator_lilyreber"
version = "0.2"
description = "A library for generating LaTeX code."
authors = [
    { name = "Liliia Imamutdinova", email = "lilyreber404@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.setuptools]
packages = ["latex_generator"]
