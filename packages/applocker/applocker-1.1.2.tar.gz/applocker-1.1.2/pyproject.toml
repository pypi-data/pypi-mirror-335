[project]
name = "applocker"
version = "1.1.2"
description = "AppLocker Policy parser and emitter for Python"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.11.2",
]
