[project]
name = "mcp-server-postgresql"
version = "0.1.6"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "truskovskiyk", email = "truskovskiyk@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.4.1",
    "psycopg2-binary>=2.9.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
mcp-server-postgresql = "mcp_server_postgresql:main"