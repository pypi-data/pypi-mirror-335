[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mlsys_graph"
version = "0.1.0"
description = "A package for graph capture functionality"
readme = "README.md"
requires-python = ">=3.6"
dependencies = []  # 필요한 의존 패키지 추가 (예: ["numpy", "matplotlib"])