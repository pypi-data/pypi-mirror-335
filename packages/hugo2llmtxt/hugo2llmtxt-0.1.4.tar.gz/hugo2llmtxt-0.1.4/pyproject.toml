[project]
name = "hugo2llmtxt"
version = "0.1.4"
description = "Generate the llms.txt file for the hugo website"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["typer>=0.15.1"]

[project.scripts]
hugo2llmtxt = "hugo2llmtxt:app"
