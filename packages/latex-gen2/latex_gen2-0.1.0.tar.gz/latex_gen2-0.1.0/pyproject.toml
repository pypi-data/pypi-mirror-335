[tool.poetry]
name = "latex-gen2"
version = "0.1.0"
packages = [
    { include = "latex_gen" }
]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
