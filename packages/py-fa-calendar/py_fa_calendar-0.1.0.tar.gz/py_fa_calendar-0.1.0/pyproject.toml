[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "py_fa_calendar"
version = "0.1.0"
description = "A Persian calendar package and countdowns"
authors = [{name = "Mahan010"}]
readme = "README.md"
requires-python = ">=3.6"
dependencies = ["datetime"]