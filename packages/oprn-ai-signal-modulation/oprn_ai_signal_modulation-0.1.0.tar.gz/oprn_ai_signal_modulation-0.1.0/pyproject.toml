[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oprn-ai-signal-modulation"
version = "0.1.0"
description = "Open Radio Network RF Modulation using AI"
readme = "README.md"
authors = [{ name="Jake", email="topsitp@gmail.com" }]
license = {text = "MIT"}
dependencies = [
]

[project.urls]
